/**
 * search.cpp
 * 
 * Search is program which find the the God's solution for given state of cube.
 * It find via brute-force search (breadth-first search) algorithm.
 */
 
#include <cube3.h>
#include <constants.h>
